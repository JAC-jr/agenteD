package com.example.agenteD.Util;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class LogsConfig {

/*    public void factorylogger (){
        Logger logger = LoggerFactory.getLogger(getClass());
    }

    public void interruptionLog(String className) {
        Logger logger = LoggerFactory.getLogger(getClass());
        logger.error("{} interruption", getClass());
    }

    public void connectionless(String className) {
        Logger logger = LoggerFactory.getLogger(getClass());
        logger.error("connection to database failure");
    }

    public void SqlexceptionLog(String className) {
        Logger logger = LoggerFactory.getLogger(getClass());
        logger.error("{} failed to get data from database", getClass());
    }
    returned data, conexion con base de datos(intento de conexion)
*/
}
